# Dependencies for the MCP servers (separate processes; NOT part of the Django
# deploy). Install into their own venv:
#
#   python -m venv .venv-mcp && . .venv-mcp/bin/activate
#   pip install -r requirements.txt          # the Django app's deps (Server B boots Django)
#   pip install -r mcp_servers/requirements.txt
#
# psycopg2-binary and sqlparse are already in the app's top-level requirements.txt;
# they are repeated here so this folder is self-describing.

mcp[cli]==1.28.1
psycopg2-binary==2.9.11
sqlparse==0.5.1

# HTTP transport (http_server.py) — hosted Render Web Service. These ship with
# mcp[cli] already; pinned explicitly so the server build is self-describing.
uvicorn==0.51.0
starlette==1.3.1

# OAuth 2.1 resource-server token verification (http_server OAuth mode)
PyJWT==2.10.1
cryptography==45.0.4

# Test-only
pytest==9.1.1
httpx==0.28.1
asgi-lifespan==2.1.0
